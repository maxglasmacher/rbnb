<%= simple_form_for([@yacht, @booking]) do |f| %>
  <%= f.error_notification %>
  <%= f.input :booking_date, input_html: {class: "datepicker"} %>
<%= f.button :submit, class: "btn-primary" %>
<% end %>
